# Changelog

Все значимые изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и этот проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

## [Unreleased]

### Добавлено (2025-11-12)
- **Пакет для корневого GitHub Pages репозитория `ksp-hub.github.io`**:
  - Создан минималистичный лендинг с мгновенным редиректом на портфолио и метатегами для SEO и верификации
  - Подготовлены `robots.txt` и `sitemap.xml` для корневого домена GitHub Pages
  - Файлы собраны в каталог `deploy/ksp-hub-github-io-root/` и упакованы в архив `archives/ksp-hub-github-io-root.zip`
  - Добавлен скрипт `scripts/create_root_zip.py` для повторной сборки архива при необходимости

### Исправлено (2025-11-10)
- **Критические исправления отображения тегов технологий и описания проектов**:
  - Убраны все ограничения `max-height` и `overflow: hidden` для тегов технологий при 100% масштабе
  - Удалена функция `initZoomDetection()`, которая добавляла классы `zoom-constrained` и `zoom-very-constrained` к body
  - Убраны CSS ограничения для масштаба браузера 50%-35%, которые вызывали обрезку тегов даже при 100% масштабе
  - Теги технологий теперь отображаются полностью при нормальном масштабе (100%)
  - Файлы: `css/components.css`, `js/main.js`
- **Исправление отображения описания проекта в карточке**:
  - Убран большой `padding-right: 6rem` из `.project-preview-mini`, который обрезал текст описания
  - Добавлена минимальная высота `min-height: 2.5em` для гарантированного отображения текста
  - Описание теперь корректно отображается в карточках проектов вместо "-"
  - Файлы: `css/components.css`
- **Восстановление эффектов hover для фильтров статусов**:
  - Добавлены стили для `.filter-group:last-child .tech-tag[data-status="ongoing"]:hover`
  - Добавлены стили для `.filter-group:last-child .tech-tag[data-status="backlog"]:hover`
  - Добавлены стили для `.filter-group:last-child .tech-tag[data-status="done"]:hover`
  - Все эффекты hover включают `transform: translateY(-2px)` и `box-shadow` с соответствующими цветами
  - Файлы: `css/components.css`
- **Полное обновление статусов на "ongoing"**:
  - Обновлен статус проекта 10 в данных: `status: 'in progress'` → `status: 'ongoing'`
  - Обновлен маппинг статусов в `js/main.js`: `'in progress'` → `'ongoing'` в `statusMap`, отображение "Ongoing"
  - Обновлен HTML атрибут в фильтрах: `data-status="in progress"` → `data-status="ongoing"` в `index.html`
  - Обновлены CSS классы: `.project-card-compact.status-ongoing::before` и `.project-status-mini.status-ongoing`
  - Обновлены CSS селекторы: `.tech-tag[data-status="in progress"]` → `.tech-tag[data-status="ongoing"]`
  - Добавлены переопределения стилей из минифицированного CSS для статуса "ongoing"
  - Обновлена фильтрация: нормализация статусов с `.trim()` для корректной работы фильтров
  - Устаревшие статусы: "doing", "in progress", "status-in-progress" полностью заменены на "ongoing"
  - Файлы: `js/main.js`, `index.html`, `css/components.css`
- **Ограничение предпросмотра описания проекта при масштабе 50%-35%**:
  - Добавлены медиа-запросы для ограничения описания до 1 строки при маленьком viewport (масштаб 50%-35%)
  - При viewport `max-width: 1400px` и `max-height: 900px`: описание ограничено до 1 строки (`-webkit-line-clamp: 1`)
  - При viewport `max-width: 1200px` и `max-height: 700px`: более строгие ограничения (1 строка, уменьшенный шрифт)
  - Уменьшены отступы снизу (`margin-bottom`) для улучшения отображения нижней части карточки (дата, номер проекта, счетчик, "Click for details")
  - Это улучшает общий внешний вид карточек проектов при маленьком масштабе браузера
  - Файлы: `css/components.css`
- **Обновление текста оборудования**:
  - Убран жирный текст из слова "Workstation:" (удален `<strong>` тег)
  - Убрано двоеточие ":" после слова "Workstation"
  - Добавлен эмодзи 🔗 перед ссылкой на Hardware.md
  - Изменен формат списка оборудования: разделители заменены на " | " (вертикальные разделители)
  - Новый формат: "Workstation Dell Precision 7920 Tower, Windows 10 Pro  |  Linux notebook  |  Macbook  |  🔗 Hardware.md"
  - Файлы: `index.html`
- **Выравнивание элементов в нижней части карточки проекта**:
  - Исправлено выравнивание элементов в `.project-meta-mini`: дата, номер проекта, счётчик просмотров и "Click for details →"
  - Добавлен `align-items: center` для вертикального выравнивания всех элементов по центру
  - Добавлен `line-height: 1.4` для всех элементов для выравнивания по базовой линии
  - Добавлен `white-space: nowrap` для предотвращения переноса текста
  - Добавлена граница сверху (`border-top: 1px solid var(--border-color)`) для визуального разделения
  - Добавлен `padding-top: 0.25rem` для уменьшенного отступа сверху (поднятие элементов выше)
  - Добавлен `gap: 0.75rem` для равномерных отступов между элементами
  - Уменьшен `margin-bottom` у `.project-preview-mini` с `0.8rem` до `0.5rem` для поднятия мета-информации
  - Все элементы теперь выровнены по центру вертикально и имеют одинаковую высоту строки (`line-height: 1.4`)
  - На мобильных устройствах (max-width: 768px) элементы отображаются вертикально с выравниванием по левому краю
  - Подсказка "Click for details →" на мобильных устройствах выравнивается по правому краю (`align-self: flex-end`)
  - Уменьшены отступы для медиа-запросов при масштабе 50%-35% для поднятия элементов
  - Файлы: `css/components.css`

### Добавлено (2025-11-10)
- **Настройка Analytics API Endpoint**:
  - Добавлена подробная документация для настройки Analytics API endpoint в `js/main.js`
  - Документация включает инструкции для трех вариантов настройки:
    - Option 1: Serverless Function (Vercel, Netlify Functions, AWS Lambda) - рекомендуется
    - Option 2: Third-party Analytics Service (Plausible, Simple Analytics)
    - Option 3: Manual Updates (ручное обновление статистики)
  - Добавлены примеры URL endpoint'ов и примечания по безопасности
  - Улучшена обработка ошибок API запросов с логированием предупреждений
  - Файлы: `js/main.js`
- **ID проекта в карточках**:
  - Добавлен минималистичный бейдж с ID проекта в верхний правый угол каждой карточки проекта
  - Формат отображения: "No. {id}" (например, "No. 1", "No. 2")
  - Бейдж без фона, с низкой непрозрачностью (opacity: 0.6 в светлой теме, 0.5 в темной)
  - Добавлен отступ сверху для header карточки (padding-top: 1.5rem) для размещения ID
  - Файлы: `js/main.js`, `css/components.css`

### Исправлено (2025-11-10)
- **Оптимизация отображения заголовков карточек проектов при масштабе 50%-35%**:
  - Исправлена проблема с "съезжанием" заголовков карточек проектов при масштабе 50%-35%
  - **Полностью переработана структура header с использованием Flexbox**:
    - `.project-header-mini` теперь использует `display: flex` с `gap` вместо `padding-right` для заголовка
    - `.project-title-mini` использует `flex: 1` и `min-width: 0` для естественного переноса текста
    - Убран фиксированный `padding-right` из заголовка (теперь `padding-right: 0`)
    - Убрано ограничение `max-width` для заголовка (теперь `max-width: none`)
    - Добавлен `.project-meta-header` с `flex-shrink: 0` для предотвращения сжатия статуса
  - **Ограничение заголовка до 2 строк**:
    - Добавлено ограничение отображения заголовка до максимум 2 горизонтальных строк с помощью `-webkit-line-clamp: 2`
    - Использован `display: -webkit-box` с `-webkit-box-orient: vertical` для корректной работы `line-clamp`
    - Добавлен `overflow: hidden` и `text-overflow: ellipsis` для обрезки длинного текста с многоточием
    - Установлена максимальная высота `max-height: 2.85em` (рассчитана как `line-height * font-size * 2`) для гарантированного ограничения до 2 строк
    - Адаптирована максимальная высота для каждого медиа-запроса в соответствии с изменением размера шрифта и высоты строки
    - Это решает проблему с "съезжанием" заголовков при масштабе 50%-35% браузера, предотвращая переполнение текста
  - Улучшен перенос текста заголовков:
    - `word-wrap: break-word` для корректного переноса длинных слов
    - `word-break: normal` для нормального переноса (не разрываем слова насильно)
    - `hyphens: auto` для автоматической расстановки переносов
    - `line-height: 1.5` для улучшенной читаемости
    - Все свойства применены с `!important` для гарантированного применения
  - Добавлены адаптивные медиа-запросы для различных размеров экранов:
    - **@media (max-width: 1000px)** - для средних экранов:
      - Уменьшен размер шрифта до `0.93rem`
      - Уменьшен gap до `0.6rem`
    - **@media (max-width: 768px)** - для мобильных устройств:
      - Уменьшен gap до `0.5rem`
      - Уменьшен размер шрифта до `0.9rem`
    - **@media (max-width: 600px)** - для узких экранов:
      - Уменьшен размер шрифта до `0.88rem`
      - Уменьшен gap до `0.4rem`
    - **@media (max-width: 480px)** - для очень маленьких экранов:
      - Уменьшен размер шрифта до `0.85rem`
      - Уменьшен gap до `0.35rem`
  - Адаптированы размеры статусов проектов для каждого медиа-запроса
  - **Решена проблема с масштабом браузера**: теперь заголовки корректно ограничены до 2 строк при любом масштабе (включая 50%-35%), предотвращая "съезжание" текста
  - **Адаптированы медиа-запросы для корректного расчета max-height**: 
    - Добавлен расчет `max-height` в каждом медиа-запросе на основе `line-height * 2` для гарантированного ограничения до 2 строк
    - @media (max-width: 1000px): `max-height: 2.8em` (line-height: 1.4 * 2)
    - @media (max-width: 768px): `max-height: 2.8em` (line-height: 1.4 * 2)
    - @media (max-width: 600px): `max-height: 2.76em` (line-height: 1.38 * 2)
    - @media (max-width: 480px): `max-height: 2.9em` (line-height: 1.45 * 2)
  - Файлы: `css/components.css`

### Проверено (2025-11-10)
- **Проверка проекта на наличие старых записей "Doing"**:
  - Проведена полная проверка проекта на наличие старых записей "Doing" и отсутствие замены на "In Progress"
  - Проверены файлы: `index.html`, `js/main.js`, `Инструкция добавления проектов.md`, `Аббревиатуры и сокращения.md`
  - **Результаты проверки**:
    - `index.html` (строки 519-528): Используется "In Progress" для отображения, внутренний `data-status="doing"` корректно обрабатывается маппингом в JavaScript
    - `js/main.js`: Все проекты используют правильный технический идентификатор `'doing'` (lowercase), отображение через `statusMap` корректно показывает "In Progress" (строки 810, 943)
    - Проект с ID 1 ("Face Detection") проверен: статус `'doing'` (lowercase) соответствует внутреннему ключу (строка 368)
    - Документация обновлена: все упоминания статуса обновлены на "In Progress" в пользовательских текстах
  - **Принцип разделения данных и представления подтвержден**:
    - Технические идентификаторы (`'doing'`) остаются в данных, HTML атрибутах (`data-status`), CSS классах
    - Пользовательский текст ("In Progress") отображается через маппинг в JavaScript (`statusMap`)
    - Нормализация статуса (`.toLowerCase()`) гарантирует корректную работу независимо от регистра входных данных
  - Файлы: `index.html`, `js/main.js`, `Инструкция добавления проектов.md`, `Аббревиатуры и сокращения.md`

### Изменено (2025-11-10)
- **Секция "ОБРАЗОВАТЕЛЬНЫЙ ВЕКТОР" - менее яркий дизайн**:
  - Изменен цвет полосы над заголовками карточек - теперь синий цвет с низкой прозрачностью (opacity: 0.35) вместо градиента
  - Уменьшена яркость карточек: более мягкие тени (0 2px 8px вместо 0 3px 10px)
  - Уменьшено движение при hover (translateY(-3px) вместо -5px)
  - Уменьшены размеры иконок в заголовках карточек (1.3rem вместо 1.5rem)
  - Уменьшена прозрачность иконок и маркеров списков (opacity: 0.8 и 0.7 соответственно)
  - Уменьшен размер шрифта заголовков карточек (1.2rem вместо 1.3rem)
  - В темной теме полоса имеет еще более низкую прозрачность (opacity: 0.3)
- **Переименование статуса проекта "Doing" в "In Progress"**:
  - Обновлен текст статуса в фильтрах проектов (`index.html`) - "Doing" заменен на "In Progress"
  - Обновлен маппинг статусов в `js/main.js` (функции `renderProjects()` и `loadMoreProjects()`) - `'doing': 'Doing'` заменен на `'doing': 'In Progress'`
  - Обновлена документация в `Инструкция добавления проектов.md` - добавлено упоминание о том, что статус `'doing'` отображается как "In Progress"
  - Адаптированы карточки проектов для более длинного текста статуса "In Progress":
    - Уменьшен размер шрифта статуса с `0.7rem` до `0.65rem` в `css/components.css`
    - Уменьшен горизонтальный padding статуса до `0.2rem 0.55rem` для компактности
    - Добавлен `white-space: nowrap`, `min-width: fit-content`, `flex-shrink: 0` для предотвращения переноса и сжатия
    - Увеличен отступ справа для заголовка проекта с `6rem` до `8.5rem`, чтобы статус "In Progress" (11 символов) не перекрывался
    - Добавлен `max-width: calc(100% - 9rem)` для заголовка проекта и `word-wrap: break-word` для длинных заголовков
    - Добавлена минимальная высота (`min-height: 2.5rem`) для `.project-header-mini` для корректного размещения статуса
  - Добавлена нормализация статуса в `js/main.js`: 
    - В функциях `renderProjects()` и `loadMoreProjects()`: `(project.status || 'done').toLowerCase()` для гарантированной работы с любым регистром
    - В функции `filterProjects()`: добавлена нормализация как для статуса проекта, так и для фильтра статуса (`toLowerCase()`) для корректной фильтрации независимо от регистра (исправлена проблема с фильтрацией при разных регистрах)
  - Адаптированы фильтры статусов для корректного отображения "In Progress" (`white-space: nowrap`, `min-width: fit-content`, увеличен padding до `0.25rem 0.6rem`)
  - Создан файл `ОБЪЯСНЕНИЕ_СТАТУС_DOING_VS_IN_PROGRESS.md` с подробным объяснением:
    - Почему технические идентификаторы (`'doing'`) остались неизменными
    - Почему отображаемый текст изменен на "In Progress"
    - Принцип разделения данных и представления
    - Преимущества такого подхода
    - Где пользователь видит "In Progress" и где остались технические идентификаторы
  - Добавлена адаптация для мобильных устройств:
    - На экранах до 768px: уменьшен размер шрифта статуса до `0.6rem`, отступ заголовка до `7rem`, `max-width: calc(100% - 7.5rem)`
    - На экранах до 480px: уменьшен размер шрифта статуса до `0.55rem`, отступ заголовка до `6.5rem`, `max-width: calc(100% - 7rem)`
    - Уменьшен размер шрифта фильтров статусов до `0.65rem` на мобильных

### Файлы (2025-11-10)
- **index.html**: Обновлен текст статуса в фильтрах проектов - "Doing" заменен на "In Progress"
- **js/main.js**: 
  - Обновлен маппинг статусов в функциях `renderProjects()` и `loadMoreProjects()` - `'doing': 'Doing'` заменен на `'doing': 'In Progress'`
  - Добавлена нормализация статуса: `(project.status || 'done').toLowerCase()` для гарантированной работы с любым регистром (например, `'Doing'`, `'doing'`, `'DOING'` все преобразуются в `'doing'`)
- **css/components.css**: 
  - Добавлены стили для секции "ОБРАЗОВАТЕЛЬНЫЙ ВЕКТОР" - изменен цвет полосы на синий с низкой прозрачностью, уменьшена яркость карточек
  - Улучшены стили для адаптации карточек проектов под более длинный текст статуса "In Progress":
    - Улучшено позиционирование `.project-header-mini` с минимальной высотой
    - Увеличен отступ справа для `.project-title-mini` до `8.5rem` (desktop), `7rem` (tablet), `6.5rem` (mobile)
    - Добавлены ограничения ширины заголовка с `max-width` и `word-wrap`
    - Оптимизирован padding и размер шрифта статуса для разных размеров экрана
  - Добавлена адаптация для мобильных устройств (медиа-запросы для 768px и 480px)
- **Инструкция добавления проектов.md**: Обновлена документация - добавлено упоминание о том, что статус `'doing'` отображается как "In Progress"
- **Аббревиатуры и сокращения.md**: Обновлена таблица статусов проектов - "Doing" заменен на "In Progress (doing)"
- **ОБЪЯСНЕНИЕ_СТАТУС_DOING_VS_IN_PROGRESS.md**: Создан новый файл с подробным объяснением различий между техническими идентификаторами (`'doing'`) и отображаемым текстом ("In Progress"), принципов разделения данных и представления, и преимуществ такого подхода
- **Changelog.md**: Задокументированы все изменения по переименованию статуса, адаптации карточек, изменению дизайна секции "ОБРАЗОВАТЕЛЬНЫЙ ВЕКТОР" и улучшению фильтрации статусов

### Исправлено (2025-11-10)
- **Фильтрация статусов проектов**:
  - Исправлена проблема с фильтрацией статусов при разных регистрах в функции `filterProjects()`
  - Добавлена нормализация статуса проекта и фильтра статуса к строчным буквам (`toLowerCase()`) для гарантированной корректной фильтрации независимо от регистра
  - Теперь фильтрация работает корректно даже если статус в данных проекта указан в другом регистре (например, `'Doing'`, `'DOING'` или `'doing'`)

### Исправлено (2025-11-10 - КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ #3)
- **Счетчик просмотров проектов (полное исправление - третья попытка)**:
  - **ПРОБЛЕМА**: Счетчик работал неправильно и по-разному в светлой и темной теме. Считал "уникальные просмотры" вместо общего количества просмотров.
  - **РЕШЕНИЕ**: Полностью переработана логика подсчета просмотров.
  - **УДАЛЕНА логика "уникальных просмотров"** - теперь считаются ВСЕ просмотры (каждое открытие модального окна увеличивает счетчик на 1)
  - **УДАЛЕН cooldown период** (`VIEW_COOLDOWN_MS`, `PROJECT_VIEWS_LAST_VIEWED_KEY`) - больше не блокирует подсчет просмотров
  - **УДАЛЕНА функция `wasProjectViewedRecently()`** - больше не проверяет, был ли проект просмотрен недавно
  - **УПРОЩЕНА функция `incrementProjectViews()`** - теперь просто увеличивает счетчик на 1 при каждом открытии модального окна, без проверки cooldown
  - **УПРОЩЕНА функция `normalizeProjectViewsData()`** - убрана нормализация данных `lastViewed`, оставлена только нормализация счетчиков просмотров
  - **УПРОЩЕНА функция `initProjectViews()`** - убрана инициализация `lastViewed`, убраны слушатели событий для `PROJECT_VIEWS_LAST_VIEWED_KEY`
  - **ГАРАНТИРОВАН единый подсчет для светлой и темной темы** - используется один ключ `localStorage` (`portfolio_project_views`)
  - **ГАРАНТИРОВАН единый источник истины** - все данные хранятся в одном месте, независимо от темы
  - Добавлены комментарии с предупреждением: "НЕ создавать отдельные ключи для светлой/темной темы!"
  - **РЕЗУЛЬТАТ**: Счетчик теперь работает одинаково в светлой и темной теме, считает все просмотры (не уникальные), увеличивается при каждом открытии модального окна

### Добавлено (2025-11-10)
- **Модульная структура компонентов**:
  - Создана папка `components/` для хранения переиспользуемых HTML компонентов
  - Создан компонент `components/header.html` - заголовок сайта
  - Создан компонент `components/footer.html` - подвал сайта
  - Создан компонент `components/consent-banner.html` - баннер согласия на аналитику
  - Компоненты готовы для будущего использования с серверной стороной или сборщиком
- **Улучшенный дизайн header**:
  - Добавлен градиентный фон (linear-gradient) для header
  - Улучшено позиционирование профильного фото (увеличено до 200x200px)
  - Добавлен класс `profile-text` для лучшей структуры текста
  - Улучшены эффекты hover для профильного фото (scale, border-color)
  - Увеличены отступы (padding: 3rem) для более просторного вида
- **Фото в секции About**:
  - Добавлено профессиональное фото в секцию "ОБО МНЕ"
  - Создан класс `.about-photo` для контейнера фото
  - Создан класс `.about-photo-img` для стилизации фото
  - Добавлена подпись под фото (`.photo-caption`)
  - Реализован flexbox layout для размещения фото и контента бок о бок
  - Добавлены эффекты hover (scale) для фото
  - Адаптивный дизайн: на мобильных устройствах фото отображается сверху
- **Новый файл компонентных стилей (`css/components.css`)**:
  - Создан отдельный CSS файл для компонентных стилей
  - Стили для секции About с фото (`.about-section`, `.about-photo`, `.about-photo-img`, `.photo-caption`, `.about-content`)
  - Улучшенные стили для header (`.profile-text`, `.profile-header`, `.profile-photo`)
  - Адаптивные стили для мобильных устройств
  - Поддержка темной темы
- **Файл-форма для предложений**:
  - Создан файл `Предложения для улучшения проекта 2025-11-10.md`
  - Форма для сбора предложений и идей по улучшению проекта
  - Шаблон для предложений
  - Инструкции по заполнению
  - Разделы для принятых предложений и предложений в работе

### Добавлено
- **Конфигурационный файл (`js/config.js`)**:
  - Централизованное хранение конфигурационных констант
  - Настройки аналитики (Yandex, Google)
  - Конфигурация проектов (пагинация, сортировка)
  - Настройки счетчика просмотров
  - Конфигурация загрузки изображений (placeholder, error placeholder, lazy loading)
  - Настройки производительности (debounce, анимации)
  - Настройки error tracking (Sentry, логирование)
  - Локализация (языки, hreflang)
  - SEO настройки (verification, OG image)
- **ESLint конфигурация (`.eslintrc.js`)**:
  - Базовая конфигурация ESLint для проверки качества кода
  - Правила для предотвращения ошибок
  - Правила стиля кода (отступы, кавычки, точки с запятой)
  - Правила для ES6+ (стрелочные функции, деструктуризация, шаблонные строки)
  - Настройки для файлов конфигурации и тестов
- **Обработка ошибок для изображений**:
  - Функция `handleImageError()` для обработки ошибок загрузки изображений
  - Функция `setupImage()` для настройки изображений с placeholder и обработкой ошибок
  - Функция `setupLazyImage()` для lazy loading изображений с использованием Intersection Observer
  - Автоматическая замена на placeholder при ошибке загрузки
  - Поддержка GIF изображений в модальных окнах
- **Lazy loading для изображений**:
  - Использование Intersection Observer API для lazy loading
  - Placeholder с анимацией загрузки (shimmer effect)
  - Fallback для браузеров без поддержки Intersection Observer
  - Настраиваемый threshold для загрузки изображений
- **FAQ schema в index.html**:
  - Добавлена FAQPage schema для улучшения SEO
  - 6 вопросов и ответов о портфолио
  - Вопросы о технологиях, контактах, опыте работы, проектах, текущем фокусе, оборудовании
- **Скрипт для автоматического обновления sitemap.xml (`scripts/update-sitemap.js`)**:
  - Автоматическое обновление даты последнего изменения (lastmod) для всех URL
  - Использование: `node scripts/update-sitemap.js` или `npm run update-sitemap`
  - Подсчет количества обновленных URL
- **Error tracking (`js/error-tracking.js`)**:
  - Базовый модуль для отслеживания ошибок
  - Поддержка интеграции с Sentry (опционально)
  - Глобальные обработчики ошибок (error, unhandledrejection)
  - Функции для ручного логирования ошибок (`logError`, `logInfo`, `logWarning`)
  - Фильтрация ошибок перед отправкой в Sentry
  - Логирование в консоль (опционально)
  - Уведомления пользователю об ошибках (опционально)
- **Анимация загрузки для изображений**:
  - CSS классы для состояний загрузки (`.image-loading`, `.image-loaded`, `.image-error`, `.image-lazy`)
  - Shimmer эффект для placeholder изображений
  - Плавные переходы между состояниями
  - Поддержка темной темы
- **package.json**:
  - Добавлены скрипты для обновления sitemap, линтинга, тестирования
  - Информация о проекте, авторе, лицензии
  - DevDependencies для ESLint и Jest

### Изменено (2025-11-10)
- **index.html**:
  - Обновлен header: добавлен градиентный фон, улучшена структура с классом `profile-text`
  - Увеличен размер профильного фото в header до 200x200px
  - Обновлена секция "ОБО МНЕ": добавлено фото с подписью, реализован flexbox layout
  - Добавлена ссылка на новый CSS файл `css/components.css`
  - Обновлены встроенные стили header: изменен фон на градиент, увеличены отступы
- **css/main.css**:
  - Стили для header обновлены (градиентный фон, увеличенные отступы)
  - Подготовка к интеграции с новым файлом `css/components.css`

### Изменено
- **index.html**:
  - Добавлен preload для `js/config.js`
  - Добавлен скрипт `js/error-tracking.js` перед `js/main.js`
  - Добавлена FAQPage schema в JSON-LD
  - Проверена корректность hreflang тегов для русского и английского языков
- **js/main.js**:
  - Добавлена поддержка CONFIG из `js/config.js`
  - Добавлены функции для работы с изображениями (`handleImageError`, `setupImage`, `setupLazyImage`)
  - Обновлена функция `openProjectModal()` для использования lazy loading изображений
  - Улучшена обработка ошибок изображений с использованием CONFIG
  - **Оптимизирован счетчик просмотров проектов**:
    - Добавлена функция `normalizeProjectViewsData()` для гарантии консистентности данных (все ключи нормализуются к строковому формату)
    - Убрана избыточная нормализация данных из `getProjectViews()` и `wasProjectViewedRecently()`
    - Оптимизирована функция `updateProjectViewsInCards()` - данные из `localStorage` получаются один раз для всех элементов
    - Добавлены слушатели событий `storage` и `focus` в `initProjectViews()` для синхронизации между вкладками
    - Улучшена синхронизация счетчиков в `renderProjects()` и `loadMoreProjects()` - нормализация данных перед обновлением, использование `requestAnimationFrame` + `setTimeout` для гарантии готовности DOM
    - Улучшена функция `incrementProjectViews()` - явно использует строковые ключи, удаляет дублирующиеся ключи
- **css/main.css**:
  - Добавлены стили для состояний загрузки изображений (`.image-loading`, `.image-loaded`, `.image-error`, `.image-lazy`)
  - Добавлена анимация shimmer для placeholder изображений
  - Добавлена поддержка темной темы для новых стилей изображений

### Файлы (2025-11-10)
- `components/header.html` - компонент заголовка сайта
- `components/footer.html` - компонент подвала сайта
- `components/consent-banner.html` - компонент баннера согласия на аналитику
- `css/components.css` - новый файл с компонентными стилями
- `Предложения для улучшения проекта 2025-11-10.md` - форма для сбора предложений

### Файлы
- `js/config.js` - новый файл с конфигурацией
- `.eslintrc.js` - новый файл с конфигурацией ESLint
- `js/error-tracking.js` - новый файл для отслеживания ошибок
- `scripts/update-sitemap.js` - новый файл для автоматического обновления sitemap.xml
- `package.json` - новый файл с метаданными проекта и скриптами

### Исправлено
- **Счетчик просмотров проектов (критическое исправление - финальная версия)**:
  - Полностью переработан механизм счетчика просмотров для устранения проблем с разными темами
  - **Удалена зависимость от `sessionStorage`** - теперь используется только `localStorage` с временными метками
  - **Проблема решена**: счетчик теперь работает одинаково для светлой и темной темы, так как использует только `localStorage`
  - Добавлена функция `normalizeProjectViewsData()` для гарантии консистентности данных - все ключи в `localStorage` нормализуются к строковому формату (так как JSON всегда использует строковые ключи)
  - Добавлен период охлаждения (1 минута) - повторные просмотры в течение этого времени не увеличивают счетчик (предотвращает двойной подсчет при случайном двойном клике или быстром закрытии/открытии, НЕ влияет на переключение темы)
  - Удалены функции `isProjectViewedInSession()` и `markProjectAsViewedInSession()` (больше не нужны, так как не используем `sessionStorage`)
  - Добавлена функция `wasProjectViewedRecently()` для проверки недавних просмотров на основе временных меток в `localStorage` (период охлаждения 1 минута)
  - Улучшена функция `getProjectViews()` - убрана избыточная нормализация данных, оптимизирована для работы со строковыми ключами (JSON всегда использует строковые ключи)
  - Улучшена функция `updateProjectViewsInCards()` - данные из `localStorage` получаются один раз для всех элементов, что гарантирует единый источник истины и предотвращает рассинхронизацию
  - Улучшена функция `incrementProjectViews()` - явно использует строковые ключи для сохранения данных, удаляет дублирующиеся ключи (числовые и строковые варианты)
  - Улучшена функция `initProjectViews()` - добавлены слушатели событий `storage` и `focus` для синхронизации между вкладками и при возврате фокуса на страницу
  - Добавлено обновление счетчиков после рендеринга проектов в `renderProjects()` и `loadMoreProjects()` с использованием `requestAnimationFrame` + `setTimeout` для гарантии готовности DOM
  - Все вызовы `updateProjectViewsInCards()` обернуты в `requestAnimationFrame` + `setTimeout` для гарантии, что DOM полностью обновлен перед синхронизацией счетчиков
  - Все ключи `localStorage` переименованы для ясности: `PROJECT_VIEWS_LAST_VIEWED_KEY` вместо `PROJECT_VIEWS_TIMESTAMP_KEY`
  - Улучшена консистентность данных - теперь всегда используются строковые ключи в объектах (так как JSON всегда использует строковые ключи), старые числовые ключи автоматически удаляются при нормализации
  - Добавлена синхронизация между вкладками через событие `storage` - при изменении данных в другой вкладке счетчики обновляются автоматически
  - Добавлена синхронизация при возврате фокуса на страницу - при возврате фокуса счетчики обновляются для отражения возможных изменений из другой вкладки

### Удалено (2025-11-10)
- **IMPROVEMENTS.md**: Удален файл, так как все улучшения реализованы и проверены
- **Предложения для улучшения проекта 2025-11-09.md**: Удален файл, так как все улучшения реализованы и проверены
- **Логика "уникальных просмотров" из счетчика просмотров проектов**:
  - Удалена константа `VIEW_COOLDOWN_MS` (1 минута) - больше не используется для блокировки подсчета
  - Удалена константа `PROJECT_VIEWS_LAST_VIEWED_KEY` - больше не хранятся временные метки последнего просмотра
  - Удалена функция `wasProjectViewedRecently()` - больше не проверяет, был ли проект просмотрен недавно
  - Удалена нормализация данных `lastViewed` из функции `normalizeProjectViewsData()`
  - Удалена инициализация `lastViewed` из функции `initProjectViews()`
  - Удалены слушатели событий для `PROJECT_VIEWS_LAST_VIEWED_KEY` из функции `initProjectViews()`
  - Удалена логика сохранения временных меток из функции `incrementProjectViews()`
  - **Причина удаления**: Логика "уникальных просмотров" создавала путаницу и не соответствовала требованиям пользователя. Теперь считаются все просмотры (каждое открытие модального окна увеличивает счетчик на 1)

### Изменено
- **IMPROVEMENTS.md** (до удаления):
  - Все улучшения помечены как выполненные (серые и зачеркнутые)
  - Добавлены детали статуса для каждого улучшения
  - Обновлен статус проекта на "Готов к продакшену"
- **Аббревиатуры и сокращения.md**:
  - Полностью переработан файл с добавлением всех сокращений из проекта
  - Добавлены разделы: Веб-технологии, Форматы изображений, SEO и доступность, Данные и аналитика, Машинное обучение, Инструменты и платформы, Оборудование и железо
  - Добавлены таблицы с кодами, статусами проектов, префиксами в коде, CSS классами, ключами хранилища
  - Обновлена дата последнего обновления на 2025-11-09

### Исправлено
- **Счетчик просмотров проектов**:
  - Исправлена проблема с раздельным подсчетом для светлой и темной темы
  - Теперь используется единый `localStorage` ключ для всех тем
  - Добавлена функция `updateProjectViewsInCards()` для обновления счетчиков в карточках после увеличения просмотров
  - Добавлены атрибуты `data-project-id` и `data-views-count` к карточкам проектов для правильного обновления счетчиков
  - Улучшена функция `getProjectViews()` для гарантированного возврата числа (не строки)
  - Исправлена инициализация `sessionStorage` - теперь не перезаписывается при перезагрузке страницы, если уже существует
- **Кнопка "Наверх" (Scroll to Top)**:
  - Исправлена проблема с видимостью кнопки
  - Улучшена функция `initScrollToTop()` - теперь правильно управляет видимостью через CSS класс `.visible`
  - Убраны конфликтующие inline стили, управление видимостью теперь полностью через CSS
  - Добавлена проверка начального состояния при загрузке страницы
  - Добавлен обработчик события `resize` для корректного отображения при изменении размера окна
  - Улучшена обработка событий прокрутки с использованием `passive: true` для лучшей производительности

### Удалено
- **Документация**:
  - Удалены реализованные улучшения из `IMPROVEMENTS.md`:
    - Пункт 1: "Обновить даты в sitemap.xml" (✅ реализовано - даты обновлены на 2025-11-09)
    - Пункт 3: "Исправить theme-color" (✅ реализовано - изменен на #2c3e50)
    - Пункт 4: "Вынести inline стили в CSS" (✅ реализовано - удалены inline стили из секции About)
    - Пункт 5: "Добавить Service Worker для PWA" (✅ реализовано - создан sw.js)
    - Пункт 6: "Улучшить обработку ошибок в JavaScript" (✅ реализовано - добавлена функция showNotification)
    - Пункт 7: "Добавить валидацию данных проектов" (✅ реализовано - добавлена функция validateProjectData)
    - Пункт 8: "Добавить preload для критичных ресурсов" (✅ реализовано - добавлены preload ссылки)
    - Пункт 9: "Оптимизировать загрузку Font Awesome" (✅ реализовано - асинхронная загрузка)
    - Пункт 12: "Добавить skip-to-content ссылку" (✅ реализовано - добавлена skip-link)
    - Пункт 13: "Улучшить ARIA labels для фильтров" (✅ реализовано - обновлены aria-label)
    - Пункт 14: "Добавить focus trap в модальных окнах" (✅ реализовано - добавлены функции trapFocus/removeFocusTrap)
    - Пункт 15: "Добавить индикатор загрузки для проектов" (✅ реализовано - добавлены функции showLoadingIndicator/hideLoadingIndicator)
    - Пункт 16: "Добавить breadcrumbs schema" (✅ реализовано - добавлена BreadcrumbList schema)
    - Пункт 29: "Улучшить аналитику (отслеживание событий)" (✅ реализовано - добавлена функция trackEvent)
    - Пункт 30: "Добавить кнопку Наверх (Scroll to Top)" (✅ реализовано - добавлена функция initScrollToTop)
    - Пункт 32: "Создать CHANGELOG.md" (✅ реализовано - файл создан и ведется)
  - Перенумерованы все оставшиеся пункты улучшений для сохранения последовательности
  - Обновлен раздел "Приоритеты внедрения" - удалены галочки с реализованных пунктов
  - Добавлены примечания о пропуске улучшений для изображений (из-за использования GIF)

### Добавлено
- **Service Worker**:
  - Создан файл `sw.js` для кэширования основных ресурсов и обеспечения офлайн-доступа
  - Регистрация Service Worker в `index.html`
- **Обработка ошибок**:
  - Добавлена функция `showNotification()` для отображения пользовательских уведомлений об ошибках, предупреждениях и информации
  - Добавлены стили для уведомлений (`.notification`, `.notification-error`, `.notification-warning`, `.notification-info`)
- **Валидация данных**:
  - Добавлена функция `validateProjectData()` для проверки обязательных полей и типов данных в объектах проектов
  - Интегрирована в функцию `init()` для валидации всех проектов при загрузке страницы
- **Аналитика**:
  - Добавлена функция `trackEvent()` для отправки событий в Google Analytics и Yandex Metrika при взаимодействии с фильтрами и модальными окнами проектов
  - Отслеживаются события: открытие/закрытие модальных окон, фильтрация по категориям, статусам и технологиям
- **Доступность**:
  - Реализован `focus trap` для модальных окон (`trapFocus()`, `removeFocusTrap()`) для улучшения навигации с клавиатуры
  - Добавлена `skip-to-content` ссылка (`.skip-link`) для быстрого перехода к основному контенту
  - Добавлен `id="main-content"` к тегу `<main>` для навигации
  - Улучшены `aria-label` атрибуты для всех фильтров в секции "МОИ ПРОЕКТЫ"
  - Добавлены `aria-live` атрибуты для динамического контента (статистика фильтров, сообщения об отсутствии проектов)
- **UI/UX**:
  - Добавлена кнопка "Наверх" (`initScrollToTop()`) с плавной прокруткой и анимацией появления/исчезновения
  - Добавлен индикатор загрузки (`showLoadingIndicator()`, `hideLoadingIndicator()`) для секции проектов при фильтрации/пагинации
  - Улучшен feedback при фильтрации:
    - Добавлено сообщение "Проекты не найдены" при отсутствии результатов
    - Улучшена статистика фильтров с отображением показанных/найденных/всего проектов
    - Добавлен класс `.error` для статистики при отсутствии проектов (красный фон)
    - Статистика всегда отображается при активных фильтрах
- **Оптимизация производительности**:
  - Добавлены `preload` ссылки для критичных ресурсов (`css/main.css`, `js/main.js`, `Font Awesome`)
  - Оптимизирована загрузка `Font Awesome` - асинхронная загрузка с `noscript` fallback
  - Управление видимостью элементов модального окна переведено на CSS классы вместо inline стилей
- **SEO**:
  - Обновлен `theme-color` мета-тег на `#2c3e50` для лучшей интеграции с браузером
  - Добавлена BreadcrumbList schema в `index.html` для улучшения навигации в поисковых системах

### Изменено
- **Счетчик просмотров проектов**:
  - Упрощена логика работы счетчика просмотров
  - Используется sessionStorage для предотвращения множественных увеличений счетчика в одной сессии
  - Счетчик работает одинаково в светлой и темной теме
  - Убрана избыточная логика обновления счетчика после закрытия модального окна
  - Счетчик увеличивается только один раз за сессию для каждого проекта

- **Секция "ТЕКУЩИЙ ФОКУС"**:
  - Перенесена в конец портфолио (после секции "ЦЕЛИ И ПЛАНЫ")
  - Убрано центрирование текста, применено стандартное выравнивание по левому краю
  - Обновлено содержимое секции с новой структурой:
    - Добавлены списки с маркерами для лучшей читаемости
    - Структурирована информация о текущих фокусах и открытости к сотрудничеству
  - Добавлен новый раздел "Оборудование" с информацией о рабочей станции
  - Добавлена точка после "Macbook" в описании оборудования
  - Ссылка на `Hardware.md` обновлена с рабочим URL и открывается в новой вкладке
  - Добавлена стилизация ссылки на Hardware.md (пунктирное подчеркивание, hover-эффекты)

- **Документация**:
  - Значительно улучшен раздел UI/UX в `IMPROVEMENTS.md`:
    - **Пункт 28 (Анимация загрузки изображений)**: Расширено описание проблемы, добавлены детальные решения с примерами кода (CSS и JavaScript), включены дополнительные улучшения (lazy loading, Intersection Observer, WebP поддержка, кэширование), добавлена поддержка темной темы
    - **Пункты 29-30**: Добавлены приоритеты, расширены описания проблем и решений, добавлены примеры кода для реализации, включены дополнительные улучшения и рекомендации

### Стили
- Добавлены стили для счетчика просмотров проектов:
  - `.project-views-mini` - минималистичный серый дизайн с низкой непрозрачностью
  - Иконка глаза и число просмотров в серых тонах, не отвлекают от основного контента
  - Поддержка темной темы с адаптированными цветами
- Удалены старые стили `.focus-banner` с центрированием
- Добавлены новые стили для секции `#focus`:
  - `.focus-content` - контейнер для основного содержимого
  - `.focus-list` - стили для списков с маркерами
  - `.equipment-section` - стили для раздела оборудования
  - `.hardware-link` - стили для ссылки на Hardware.md (пунктирное подчеркивание, плавные переходы)
- Добавлена поддержка темной темы для всех новых элементов
- Добавлены адаптивные стили для мобильных устройств
- **Новые стили**:
  - `.skip-link` для скрытия ссылки "Перейти к основному контенту" и отображения по фокусу
  - `.modal-section-hidden` и `.modal-section-visible` для управления видимостью секций модального окна
  - Стили для `ul` и `li` в `.about-section` для замены inline стилей
  - `.project-link-hidden` для скрытия кнопок ссылок проектов
  - `.scroll-to-top-btn` для кнопки "Наверх" (позиционирование, анимации, адаптивность)
  - `.loading-indicator` для индикатора загрузки (спиннер, текст, фон)
  - `.notification`, `.notification-error`, `.notification-warning`, `.notification-info` для стилизации всплывающих уведомлений
  - `.no-projects-message` для сообщения об отсутствии проектов (иконка, заголовок, текст)
  - `.filter-stats.error` для отображения ошибки в статистике фильтров (красный фон)

### Файлы
- `js/main.js`:
  - Добавлены функции `getProjectViews()` и `incrementProjectViews()` для работы со счетчиком просмотров
  - Добавлена функция `initProjectViews()` для инициализации счетчика при загрузке страницы
  - Упрощена логика счетчика: используется sessionStorage для предотвращения множественных увеличений счетчика в одной сессии
  - Счетчик просмотров увеличивается только один раз за сессию для каждого проекта (независимо от темы)
  - Убрана функция `updateProjectViewsInCard()` - счетчик обновляется автоматически при следующем рендере карточек
  - Интегрирован счетчик просмотров в функции `renderProjects()` и `loadMoreProjects()`
  - Счетчик просмотров работает одинаково в светлой и темной теме
  - Добавлены функции `showNotification()`, `validateProjectData()`, `trackEvent()`, `trapFocus()`, `removeFocusTrap()`, `initScrollToTop()`, `showLoadingIndicator()`, `hideLoadingIndicator()`
  - Интегрированы новые функции в `init()`, `openProjectModal()`, `closeProjectModal()`, `handleCategoryClick()`, `handleStatusClick()`, `handleTechClick()`, `renderProjects()`, `loadMoreProjects()`
  - Изменено управление видимостью элементов в `openProjectModal()` с `style.display` на классы `project-link-hidden`, `modal-section-hidden`, `modal-section-visible`
  - Улучшена функция `updateFilterStats()`:
    - Добавлено отображение сообщения "Проекты не найдены" при отсутствии результатов
    - Улучшена статистика с отображением показанных/найденных/всего проектов
    - Добавлен класс `.error` для статистики при отсутствии проектов
    - Статистика всегда отображается при активных фильтрах
  - Добавлена проверка на отсутствие проектов в `renderProjects()` с отображением сообщения `.no-projects-message`
- `css/main.css`: 
  - Добавлены стили для счетчика просмотров проектов (`.project-views-mini`)
  - Добавлены стили для `.skip-link`, `.modal-section-hidden`, `.modal-section-visible`, `.about-section ul`, `.about-section ul li`, `.project-link-hidden`, `.scroll-to-top-btn`, `.loading-indicator`, `.notification`, `.no-projects-message`, `.filter-stats.error`
- `index.html`: 
  - Обновлена структура секции "ТЕКУЩИЙ ФОКУС", добавлена рабочая ссылка на Hardware.md
  - Обновлен `theme-color` мета-тег на `#2c3e50`
  - Добавлены `preload` ссылки для `css/main.css`, `js/main.js`, `Font Awesome`
  - `Font Awesome` теперь загружается асинхронно с `noscript` fallback
  - Добавлена `skip-to-content` ссылка (`.skip-link`)
  - Добавлен `id="main-content"` к тегу `<main>`
  - Удалены inline стили из списка компетенций в секции "ОБО МНЕ"
  - Удален `style="display:none;"` из `consent-banner`
  - Модальные секции (`features`, `results`, `metrics`) теперь скрываются/показываются с помощью классов `modal-section-hidden`
  - Улучшены `aria-label` для всех фильтров в секции "МОИ ПРОЕКТЫ"
  - Добавлена регистрация Service Worker
  - Добавлена BreadcrumbList schema для SEO
- `sw.js`: 
  - Создан новый файл Service Worker для кэширования основных ресурсов и обеспечения офлайн-доступа
- `IMPROVEMENTS.md`: 
  - Расширен раздел UI/UX улучшений с детальными описаниями и примерами кода
  - Удалены реализованные улучшения (кроме улучшений для изображений)
  - Перенумерованы оставшиеся пункты улучшений
  - Обновлен раздел "Приоритеты внедрения"

---

## [Предыдущие изменения]

### Добавлено
- **Счетчик просмотров проектов**:
  - Добавлен счетчик просмотров с иконкой глаза в карточках проектов
  - Счетчик отображается между датой проекта и текстом "Click for details"
  - Просмотры отслеживаются в localStorage и сохраняются между сессиями
  - Счетчик увеличивается при каждом открытии модального окна проекта
  - Минималистичный серый дизайн, не отвлекает от основного контента
  - Поддержка темной темы
- Секция "ОБРАЗОВАТЕЛЬНЫЙ ВЕКТОР" с карточками обучения
- Секция "ЦЕЛИ И ПЛАНЫ" с обновленным текстом
- Система пагинации для проектов с кнопкой "Показать еще"
- Сортировка проектов по дате (новые сверху)
- Бейдж с общим количеством часов работы над проектами
- Фильтры по статусам проектов (Backlog, In Progress, Done)
- Поддержка новых категорий проектов: "Компьютерное зрение" и "Глубокое обучение"
- 8 новых проектов (ID 10-17) в портфолио

### Исправлено
- Видимость текста в темной теме для вводного параграфа в секции проектов
- Видимость статусов "Backlog" и "In Progress" в карточках проектов в темной теме
- Позиционирование кнопки закрытия модального окна проекта
- Интервалы между группами фильтров в секции "МОИ ПРОЕКТЫ"
- Обрезка тегов в фильтрах технологий
- Прокрутка тегов технологий (вертикальная с двумя строками)
- Обработка пустых полей `image`, `link`, `repo` в модальном окне проекта

### Обновлено
- Даты в `sitemap.xml` обновлены на актуальные
- Текст в секции "ОБРАЗОВАТЕЛЬНЫЙ ВЕКТОР"
- Текст в секции "ЦЕЛИ И ПЛАНЫ" (обновлены формулировки целей)
- Стили статусов проектов в фильтрах для соответствия карточкам проектов

---

## Формат записей

- **Добавлено** - для новых функций
- **Изменено** - для изменений в существующей функциональности
- **Устарело** - для функций, которые скоро будут удалены
- **Удалено** - для удаленных функций
- **Исправлено** - для исправления ошибок
- **Безопасность** - в случае уязвимостей

